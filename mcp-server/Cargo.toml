[package]
name = "meeting-notes-mcp"
version = "0.1.0"
edition = "2021"
description = "Local MCP server for Myna Notes (meetings-mcp-snapshot.json)"

[[bin]]
name = "meeting-notes-mcp"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
